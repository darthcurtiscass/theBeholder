<section class="container">
  <h3 class="campaign-name">{{campaign.name}}</h3>

  {{#if loggedIn}}
  <section class="flex-row justify-center">
    {{#each campaign-name as |campaign|}}
    {{> campaign-details}}
    {{/each}}
  </section>
</section>
{{else}}
<a href="../public/js/login.js">You must login first to view this page</a>
{{/if}}