<section class="container">
  <h3 class="charecter-name">{{charecter.name}}</h3>

  {{#if loggedIn}}
  <section class="flex-row justify-center">
    {{#each charecter-name as |charecter|}}
    {{> charecter-details}}
    {{/each}}
  </section>
</section>
{{else}}
<a href="../public/js/login.js">You must login first to view this page</a>
{{/if}}